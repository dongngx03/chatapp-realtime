import { createContext} from "react";

const ChatRoomContext = createContext<any>({});

export default ChatRoomContext;

//  handleSearchFriend: (e: React.FormEvent<HTMLFormElement>) => Promise<void>
//  setEmail: Dispatch<SetStateAction<string>>,
